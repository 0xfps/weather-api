import { Router } from "express"
import { empty } from "../utils/empty"

const llWeatherRouter: Router = Router()

llWeatherRouter.post("/location", (req, res) => {

})

export default llWeatherRouter